Now let’s move to the employee dashboard overview.

1. Go to:
   `src/pages/user/Overview.tsx`

2. Implement the employee dashboard metrics/stats section.

   * Use the same endpoint structure/pattern as the admin dashboard overview.
   * However, the employee response shape is different.

3. Employee stats response:

   ```json
   {
     "employeeAssignedDeployments": 3,
     "employeeCompletedDeployments": 1,
     "employeePendingMaterials": 50,
     "employeeInTransitMaterials": 0,
     "recentActivities": []
   }
   ```

4. Build the employee overview UI using the same dashboard design system already used in admin overview:

   * same card styling
   * same spacing
   * same typography hierarchy
   * same responsive layout
   * same empty/loading/error states

5. Suggested metrics cards:

   * Assigned Deployments
   * Completed Deployments
   * Pending Materials
   * In-Transit Materials

6. For recent activities:

   * Reuse the existing reusable activities/list architecture already implemented.
   * Avoid creating a separate employee-specific activities component.
   * Handle proper empty state if `recentActivities` is empty.

7. Go to:
   `src/pages/user/Deployments.tsx`

8. Employee deployments page:

   * Use the same deployments endpoint as admin.
   * Reuse the reusable table component already created.

9. Important difference for employee deployment table:

   * Employees should ONLY have:

     * `View` action (existing view action)
   * Do not add: 

     * edit
     * delete
     * admin-only actions
     * status update actions

 

10. One last missing module: 
    `src/pages/admin/Employees.tsx`

121 Implement employee management for admin. 

12 Create employee: 
    Endpoint:

```txt
POST /auth/employees
```

13 Request body: 

```json
{
  "name": "Folagbade Abdulazeem",
  "email": "azeem@gmail.com",
  "password": "12345678"
}
```

14. Get employees: 

* Use the same endpoint already used for the reusable searchable select while assigning deployments:

```txt
GET /auth/employees
```

15. Employees page architecture: 

* Use the reusable table component.
* No separate single/details page is needed.
* All employee details can remain in the table view.

16. Employee response structure: 

```json
{
  "employee": {
    "name": "Folagbade Abdulazeem",
    "email": "azeem@gmail.com",
    "role": "employee",
    "_id": "6a033f67cbe4ebc848e36b8a",
    "createdAt": "2026-05-12T14:55:35.193Z",
    "updatedAt": "2026-05-12T14:55:35.193Z",
    "__v": 0
  }
}
```

17. Employees page features: 

* Add employee modal
* Loading/skeleton states
* Empty state UI
* Error handling
* Refetch employee list after successful creation

18. Input styling: 

* Continue using the shared input styling already used throughout the app:

  * same borders
  * same colors
  * same radius
  * same focus states

19. Overall: 

* Keep layouts/styles fully consistent
* Maximize reusable components
* Avoid duplicated logic
* Keep admin and employee dashboards scalable and maintainable